const express = require('express')
const router = express.Router()

router.get('/', (req, res)=>{
  // HER KOMMER NOGET RESPONSE NOGET
})

router.get('/createDemo', (req, res)=>{

})

module.exports = router